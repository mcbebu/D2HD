# Common
Waypoint:
  type: oject
  required:
    - address
    - is_completed
  properties:
    address:
      description: String that describes address
      type: String
      example: '1.3521 N, 103.8198'
    is_completed:
      description: boolean value for whether a driver has finished or delivered the product
      type: boolean

# Driver


# Consignee
Consignee:
  type: object
  required:
    - address

#API
CheckDriverCompleted:
  type: object
  required:
    - completed_waypoint_list
  properties:
    completed_waypoint_list:
      type: array
      items:
        $ref: ''


